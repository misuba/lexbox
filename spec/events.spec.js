/*
describe("events", function() {
  var mongoose = require('mongoose'),
      db = mongoose.connect("mongodb://localhost/lexbox-test"),
      LText = db.model('LText', require('../models/texts').schema);
      LBox = db.model('LText', require('../models/boxes').schema);
      LEvent = db.model('LText', require('../models/events').schema);

  it("logs text creation");

  it("logs box creation");

  it("logs box insertion of a group of texts");

  it("logs box removal of a group of texts");

  it("gets last 50 historic events");

});
*/